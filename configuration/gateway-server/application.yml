server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
      - id: order-service
        predicates:
        - Path=/order/**
        filters:
        - StripPrefix=false
      - id: query-service
        predicates:
        - Path=/query/**
        filters:
        - StripPrefix=false
      - id: axon-webui
        predicates:
        - Path=/ui/**
        filters:
        - StripPrefix=false

eureka:
  client:
    fetchRegistry:true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
